AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Project 01 - Foundational VPC Network — EXERCISE VERSION
  Complete the Resources section below.
  Parameters and Outputs are provided for you.

# ============================================================
# Parameters — already complete, read before writing Resources
# ============================================================

Parameters:
  ProjectName:
    Type: String
    Default: cloud-engineer-01

  VpcCidr:
    Type: String
    Default: 10.0.0.0/16

  PublicSubnet1Cidr:
    Type: String
    Default: 10.0.1.0/24

  PublicSubnet2Cidr:
    Type: String
    Default: 10.0.2.0/24

  PrivateSubnet1Cidr:
    Type: String
    Default: 10.0.10.0/24

  PrivateSubnet2Cidr:
    Type: String
    Default: 10.0.20.0/24

  AdminIpCidr:
    Type: String
    Description: Your public IP in CIDR notation (e.g. 203.0.113.10/32)
    AllowedPattern: '^(\d{1,3}\.){3}\d{1,3}/\d{1,2}$'

  KeyPairName:
    Type: AWS::EC2::KeyPair::KeyName

  BastionAmiId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/ami-amazon-linux-latest/amzn2-ami-hvm-x86_64-gp2

  BastionInstanceType:
    Type: String
    Default: t3.micro
    AllowedValues: [t3.micro, t3.small, t3.medium]

# ============================================================
# Resources — YOUR WORK
#
# Write all resource blocks here.
# Use the logical IDs from the exercise README and the
# Outputs section below to know how resources should be named.
#
# Refer to the AWS CloudFormation Resource Reference:
# https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-template-resource-type-ref.html
# ============================================================

Resources:
  # TODO: Write all resource blocks here
  # Start with the VPC, then subnets, then gateways, then route tables,
  # then security groups, then the EC2 instance.
  # Each resource depends on the ones before it — work top to bottom.

  PlaceholderDoNotDeploy:
    Type: AWS::CloudFormation::WaitConditionHandle
    # Remove this placeholder once you have written your first real resource.
    # CloudFormation requires at least one resource in the Resources section.

# ============================================================
# Outputs — already complete
# These tell you how your resources should be referenced.
# ============================================================

Outputs:
  VpcId:
    Description: ID of the VPC
    Value: !Ref VPC
    Export:
      Name: !Sub ${ProjectName}-VpcId

  PublicSubnet1Id:
    Description: ID of Public Subnet 1
    Value: !Ref PublicSubnet1
    Export:
      Name: !Sub ${ProjectName}-PublicSubnet1Id

  PublicSubnet2Id:
    Description: ID of Public Subnet 2
    Value: !Ref PublicSubnet2
    Export:
      Name: !Sub ${ProjectName}-PublicSubnet2Id

  PrivateSubnet1Id:
    Description: ID of Private Subnet 1
    Value: !Ref PrivateSubnet1
    Export:
      Name: !Sub ${ProjectName}-PrivateSubnet1Id

  PrivateSubnet2Id:
    Description: ID of Private Subnet 2
    Value: !Ref PrivateSubnet2
    Export:
      Name: !Sub ${ProjectName}-PrivateSubnet2Id

  BastionSecurityGroupId:
    Description: ID of the bastion security group
    Value: !Ref BastionSecurityGroup
    Export:
      Name: !Sub ${ProjectName}-BastionSgId

  PrivateSecurityGroupId:
    Description: ID of the private instance security group
    Value: !Ref PrivateInstanceSecurityGroup
    Export:
      Name: !Sub ${ProjectName}-PrivateSgId

  BastionPublicIp:
    Description: Public IP of the bastion host
    Value: !GetAtt BastionHost.PublicIp

  SshCommand:
    Description: SSH command to connect to the bastion
    Value: !Sub ssh -i ~/.ssh/${KeyPairName}.pem ec2-user@${BastionHost.PublicIp}
